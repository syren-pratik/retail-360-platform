import AskShell from './AskShell';

export const metadata = {
  title: 'Ask anything | Retail 360',
  description: 'Conversational analytics over your retail data',
};

export default function AskPage() {
  return <AskShell />;
}
