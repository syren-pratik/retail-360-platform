import AgentHub from './AgentHub';

export const metadata = {
  title: 'Agent Hub | Retail 360',
  description: 'Browse and run AI agents with generative UI output',
};

export default function AgentsPage() {
  return <AgentHub />;
}
